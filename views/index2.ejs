<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/materialize/1.0.0/css/materialize.min.css">
    <title>OCR | LenDenClub</title>
</head>
<style>
    .card-sample {
        margin: 0 auto;
        max-width: 300px;
        width: 100%;
    }
</style>

<body>
    <div class="container">
        <h1>Upload File here</h1>
        <%if (!typeof msg) { %>
            <div class="card-info">
                <div class="row">
                    <div class="card-sample">
                        <div class="card">
                            <div class="card-image">
                                <img src="/images/panStructure.jpg">
                            </div>
                            <div class="card-content teal lighten-2">
                                <p>Please select clear picture of the document to upload, as displayed above.</p>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
            <% } %>

                <%= typeof msg !='undefined' ? msg: '' %>
                    <form action="/upload" method="POST" enctype="multipart/form-data">
                        <div class="file-field input-field">
                            <div class="btn grey">
                                <span>File</span>
                                <input name="myImage" type="file">
                            </div>
                            <div class="file-path-wrapper">
                                <input class="file-path validate" type="text">
                            </div>
                        </div>
                        <button type="submit" class="btn">Upload</button>
                    </form>
                    <br>
                    <div class="imageWrapper" style="max-width: 400px; width: 100%; margin: 0 auto;">
                        <img src="<%= typeof file != 'undefined' ? file: '' %>" class="responsive-img" />
                    </div>
                    <%if (typeof file !='undefined' ) { %>
                        <div class="Helllooo">
                            <%= text %>
                        </div>
                        <% } %>
    </div>
    <script src="https://code.jquery.com/jquery-3.5.1.min.js"></script>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/materialize/1.0.0/js/materialize.min.js"></script>
</body>

</html>